{
  "name": "tid-max-native",
  "version": "0.1.0",
  "private": true,
  "description": "Concha nativa (Capacitor) de TID-MAX: envuelve la PWA de tid-max/app y lee el tanque de salud del telefono (Apple Salud en iOS, Health Connect en Android).",
  "type": "module",
  "scripts": {
    "build": "node scripts/sync-web.mjs",
    "sync": "npm run build && cap sync",
    "ios": "npm run sync && cap open ios",
    "android": "npm run sync && cap open android",
    "doctor": "cap doctor"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor/android": "^6.1.0",
    "@capacitor/ios": "^6.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
