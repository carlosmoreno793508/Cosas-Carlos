# WHOOP — credenciales de la app "GAEL SYNC"
# Copia este archivo a  .env  y rellena los valores.
# NUNCA subas el .env real al repo (ya esta ignorado en .gitignore).

# Del portal developer-dashboard.whoop.com  ->  app  ->  Credentials
WHOOP_CLIENT_ID=pega-aqui-el-client-id
WHOOP_CLIENT_SECRET=pega-aqui-el-client-secret

# Debe coincidir EXACTO con el Redirect URL registrado en la app de WHOOP.
WHOOP_REDIRECT_URI=http://localhost:8765/callback

# Lo genera whoop_auth.py automaticamente (dejalo vacio la primera vez).
WHOOP_REFRESH_TOKEN=


# ---------------------------------------------------------------------------
# Polar AccessLink — para bajar tus entrenamientos de Polar Flow (H10, etc.)
# Registra una app (gratis) en  https://admin.polaraccesslink.com  ->  tu app.
# El Redirect URI debe coincidir EXACTO con el que registres en el portal.
POLAR_CLIENT_ID=pega-aqui-el-client-id
POLAR_CLIENT_SECRET=pega-aqui-el-client-secret
POLAR_REDIRECT_URI=http://localhost:8765/callback

# Los genera polar_auth.py automaticamente (dejalos vacios la primera vez).
POLAR_ACCESS_TOKEN=
POLAR_USER_ID=


# ---------------------------------------------------------------------------
# Junction (ex-Vital) — AGREGADOR: UNA API para WHOOP + Polar + Garmin + Oura +
# Fitbit + Strava + Apple + ... (300+ dispositivos). El atleta conecta su marca
# en un widget que hospeda Junction; nosotros solo bajamos los datos ya normalizados.
# Saca la API key de tu dashboard:  https://app.junction.com  >  API Keys.
JUNCTION_API_KEY=pega-aqui-tu-api-key

# COPIA la base EXACTA que te muestra tu dashboard (cambia por region/entorno).
# Sandbox US (default): https://api.sandbox.us.tryvital.io
# Produccion US:        https://api.us.tryvital.io   (EU: cambia 'us' por 'eu')
JUNCTION_API_BASE=https://api.sandbox.us.tryvital.io
JUNCTION_LINK_BASE=https://link.tryvital.io
JUNCTION_ENV=sandbox
JUNCTION_REGION=us

# Solo para el webhook (Fase 2): secreto de firma Svix, de tu portal de webhooks.
JUNCTION_WEBHOOK_SECRET=
